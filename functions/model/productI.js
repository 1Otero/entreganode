module.exports= class product {
    constructor(_idProduct, name, description, cantidad, precio){
     this._idProduct= _idProduct
     this.name= name
     this.description= description
     this.cantidad= cantidad
     this.precio= precio
    }
}